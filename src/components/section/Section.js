import SearchForm from "../search/SearchForm";

const Section = () => {
    return(
        <section className="card users-container">
           {/* Search bar componen - search form*/ } 
            <SearchForm />

            {/* Table component */ } 
            
        </section>
    )
}

export default Section;